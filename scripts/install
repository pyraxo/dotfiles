#!/bin/bash
#
# scripts/install - Wrapper for the main installer
#
# This script has been consolidated into install.sh at the repo root.

set -eu

REPO_DIR=$(cd "$(dirname "$0")/.." && pwd)

echo "Running main installer..."
exec bash "$REPO_DIR/install.sh" "$@"
